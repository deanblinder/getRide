import React from 'react';
import { View, Text } from 'native-base';
const EditRide = (props: any) => {
  const { ride } = props.route.params;
  return (
    <View>
      <Text>dfdf</Text>
    </View>
  );
};
export default EditRide;
